\newcommand{\PreEffLength}{4.25em}
\newcommand{\PreLength}   {4em}
\newcommand{\EffLength}   {4em}
\newcommand{\ActionWidth} {6em}
\newcommand{\ActionHeight}{5em}

\scheme{pickup}{1}{
  text = {\textbf{pickup}\\(#1)},
  pres = {
    {gF},
    {onT(#1)},
    {clr(#1)}
  },
  effs = {
    {{$\neg$gF}},
    {gH(#1)},
    {{$\neg$clr(#1)}},
    {{$\neg$onT(#1)}}
  },
  limb length = \PreEffLength,
  pre length  = \PreLength,
  width       = \ActionWidth,
  height      = \ActionHeight
}

\scheme{putdown}{1}{
  text = {\textbf{putdown}\\(#1)},
  pres = {
    {gH(#1)}
  },
  effs = {
    {gF},
    {{$\neg$gH(#1)}},
    {onT(#1)},
    {clr(#1)}
  },
  pre length  = \PreLength,
  eff length  = \EffLength,
  width       = \ActionWidth,
  height      = \ActionHeight
}

\scheme{stack}{2}{
  text = {\textbf{stack}\\(#1,#2)},
  pres = {
    {gH(#1)},
    {clr(#2)}
  },
  effs = {
    {gF},
    {{$\neg$gH(#1)}},
    {on(#1,#2)},
    {{clr(#1)}},
    {{$\neg$clr(#2)}}
  },
  limb length = \PreEffLength,
  pre length  = 3.5 em,
  width       = \ActionWidth,
  height      = \ActionHeight
}

\scheme{unstack}{2}{
  text = {\textbf{unstack}\\(#1,#2)},
  pres = {
    {gF},
    {clr(#1)},
    {on(#1,#2)}
  },
  effs = {
    {{$\neg$gF}},
    {gH(#1)},
    {{$\neg$on(#1,#2)}},
    {{$\neg$clr(#1)}},
    {{clr(#2)}}
  },
  limb length = \PreEffLength,
  pre length  = \EffLength,
  width       = \ActionWidth,
  height      = \ActionHeight
}

\scheme{init}{0}{
  text = {\hspace*{-2mm}}, % otherwise, the empty string is at least approx 4 mm wide
  pres = {},
  effs = { {gF} , {onT(B)} , {onT(C)} , {onT(D)} , {clr(A)} , {clr(B)} , {clr(D)} , {on(A,C)} },
  height     = 9em,
  width      = 0.75mm,
  eff length = 4em
}

\scheme{goal}{0}{
  text = {\hspace*{-2mm}}, % otherwise, the empty string is at least approx 4 mm wide
  pres = { {on(A,B)} , {on(D,C)} },
  effs = {},
  height     = 9em,
  width      = 0.75mm,,
  pre length = 4em
}
